//! Enum for colors.
enum class Color {
	BLACK = 0, /*!< Enum value BLACK. */
	BLUE, /*!< Enum value BLUE. */
	GREEN, /*!< Enum value GREEN. */
	RED, /*!< Enum value RED. */
	WHITE /*!< Enum value WHITE. */
};
